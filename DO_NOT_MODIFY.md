# 🔒 DUET 禁止修改清單（精簡版）
**快速檢查用 - 這些區域已確認正確，不應修改**

---

## ❌ 絕對不要碰的程式碼

### duet-frontend-final.html
```
Line ~984: camera.up.set(0, 0, 1)           // Z-up 設定
Line ~986: camera.position.set(50,-70,15)   // 相機位置
Line ~1105-1127: 旋轉矩陣邏輯                // Letter 旋轉（applyMatrix4）
Line ~1022: showInitialSphere()             // 初始金球
Line ~1000: ambientLight(0xffffff, 0.9)    // 環境光亮度（已優化）
Line ~1390: innerRadius=2.0, tubeRadius=0.7 // 墜頭尺寸（前後端一致）
Line ~1840: thumbnailCamera Z-up設定        // 購物車縮圖站立
Line ~937: allFonts = [...] 100種精選字體   // 前後端都支援的字體清單
```

### backend/scad_generator.py
```
Line ~22-24: bail_radius=2.0, bail_tube=0.7          // 墜頭尺寸（前後端一致）
Line ~24: bail_rotation_with_offset = +90            // 墜頭旋轉偏移
Line ~30-32: pos_x/y/z 直接對應（Z-up無需映射）     // 座標系統統一
Line ~49-53: letter1_shape() rotate([90,0,0])        // Letter 1 旋轉
Line ~56-60: letter2_shape() 外層[0,0,90] 內層[90,0,0] // Letter 2 旋轉順序
Line ~69: rotate([0,0,bail_rotation]) // 註解       // 必須用 // 不是 #
```

### backend/app.py
```
Line ~70-95: get_available_fonts() 提取純字體名稱   // 移除路徑和style
Line ~100-145: /list-fonts API 相同邏輯             // 字體清單API
Line ~165-181: if 'bailX' in data 參數解析          // 扁平格式支援
```

### backend/Dockerfile
```
Line ~17-24: git clone Google Fonts (完整 clone，不用 sparse-checkout)
```

---

## ✅ 已解決問題（2025-12-16 更新）

### **1. Z-up 座標系統完全統一**
- 前後端都使用 Z-up
- 不再需要座標映射
- pos_x/y/z 直接對應

### **2. 字體系統完整修復**
- 後端提取純字體家族名稱（移除路徑和style）
- 前端精選 100 種前後端都支援的字體
- 移除動態字體驗證（預先驗證過）
- 字體清單：Abel, Abril Fatface, ... Zilla Slab (100種)

### **3. 墜頭系統完全對齊**
- 內圈半徑：2.0mm（前後端一致）
- 管子粗細：0.7mm（前後端一致）
- 旋轉偏移：+90°（前端 rotateZ(90°) = 後端 bail_rotation + 90）
- 位置映射：Z-up 統一後直接對應

### **4. OpenSCAD 註解語法**
- **關鍵修正**：SCAD 檔案中的註解必須用 `//` 不是 `#`
- Python 的 `#` 註解會被寫入 SCAD 檔案導致語法錯誤

### **5. UI 優化**
- 環境光從 0.5 提升到 0.9（更明亮）
- 購物車縮圖使用 Z-up + 主相機角度（站立顯示）
- 字體選擇器顯示 100 種精選字體

### **6. Git 工作流程優化**
- 添加 .gitignore（排除 .DS_Store）
- 避免在 GitHub 網頁直接上傳（會導致 pull 衝突）
- 標準流程：git add → commit → push

---

## 🚧 已完成（不再進行中）

- ✅ **字體驗證系統** - 已完全修復
- ✅ **前後端座標系統** - 已統一為 Z-up
- ✅ **墜頭尺寸和旋轉** - 已對齊

---

## 📝 使用方式

**修改前快速檢查：**
1. 看要改的檔案 + 行號
2. 是否在上面清單？
3. 是 → ❌ 停止，找其他方法
4. 否 → ✅ 可以修改

**Token 成本：約 500-800 tokens**（比完整版省 80%）

---

## 🎯 當前系統狀態（2025-12-16）

### **完全正常運作**
- ✅ 前端 3D 預覽（Z-up, 100種字體）
- ✅ 後端 STL 生成（manifold geometry）
- ✅ 墜頭位置、尺寸、旋轉（前後端一致）
- ✅ 購物車縮圖（站立顯示）
- ✅ 字體系統（前後端都支援）

### **已部署**
- Frontend: 本地檔案（未來部署到 Vercel）
- Backend: Render (duet-backend-wlw8.onrender.com)
- Database: 待實作（Supabase）

---

**Last Updated: 2025-12-16 02:00 AM**
